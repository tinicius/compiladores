program Leitura;

var
  goiaba: integer;
  nome: string;
  x,y: integer;

begin
  read(goiaba);
  writeln("O valor lido foi: ", goiaba);
  readln(nome);
  writeln("O valor lido foi: ",nome);
  x := 1;
  y := x;
  write(y);
  
end.